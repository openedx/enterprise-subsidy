# .readthedocs.yml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# uv isn't natively supported by Read the Docs, so drive the whole build
# ourselves via a custom command (RTD's documented escape hatch for tools
# it doesn't integrate with directly).
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - pip install uv
    - uv sync --group doc
    - uv run sphinx-build -b html docs/ $READTHEDOCS_OUTPUT/html
